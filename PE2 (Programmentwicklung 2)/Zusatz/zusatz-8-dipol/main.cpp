#include <iostream>
#include "dipol.hpp"

using namespace std;


int main(void) {

    /*  Werte aus Aufgabe 6

    complex<double> r1 = resistor(20);
    complex<double> r2 = resistor(50);
    complex<double> r3 = resistor(50);
    complex<double> r4 = resistor(30);
    complex<double> l1 = inductor(0.5, omega);
    complex<double> l2 = inductor(1, omega);
    complex<double> c1 = capacitor(0.0005, omega);
    complex<double> c2 = capacitor(0.0001, omega);

    complex<double> temp1 = serial(r3, l2);
    complex<double> temp2 = parallel(temp1, c2);
    complex<double> temp3 = serial(temp2, r4);
    complex<double> temp4 = parallel(temp3, r2);
    complex<double> temp5 = parallel(c1, temp4);
    complex<double> temp6 = serial(l1, temp5);
    complex<double> result = serial(temp6, r1);

    cout << result << endl;
    */

    return 0;
}


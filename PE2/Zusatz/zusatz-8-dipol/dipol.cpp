#include "dipol.hpp"

/* Funktionen aus Aufgabe 6

complex<double> resistor(double r) {
    return complex<double>(r, 0);
}

complex<double> inductor(double l, double omega) {
    return complex<double>(0, omega * l);
}

complex<double> capacitor(double c, double omega) {
    complex<double> e(1, 0);
    return e / complex<double>(0, omega * c);
}

complex<double> serial(complex<double> r1, complex<double> r2) {
    return r1 + r2;
}

complex<double> parallel(complex<double> r1, complex<double> r2) {
    complex<double> p1 = r1 * r2;
    complex<double> p2 = r1 + r2;
    complex<double> result = p1 / p2;
    return result;
}

*/